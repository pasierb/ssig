import { h, Component } from "preact";

export default class EmbedTool extends Component {
  state = {
    fields: [] // { layerId: String, fieldName: String }
  }

  render(props) {
    const { layers } = props;

    
  }
}
